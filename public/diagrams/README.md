Place published SVG, PNG or WebP maths diagrams here. For a diagram exported
from LaTeX/TikZ, upload the exported image, not the `.tex` source. Reference it
as `/diagrams/your-file.svg` inside a written solution. Include descriptive
alt text and a caption where helpful. This folder is public.
